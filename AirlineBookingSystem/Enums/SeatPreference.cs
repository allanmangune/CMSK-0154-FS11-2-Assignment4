namespace FlightBookingSystem.Enums
{
    /// <summary>
    /// Represents a passenger's seat preference.
    /// </summary>
    public enum SeatPreference
    {
        Window, // Passenger prefers a window seat
        Aisle,  // Passenger prefers an aisle seat
        None    // Passenger has no specific seat preference
    }
}
